# Warning: Data directory must never be in web root or git
data/
/data/
goldeneye-data/
/var/www/goldeneye-data/

# macOS system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Apache configuration files - examples are tracked, but ignore local customizations
.htaccess.local
backups/.htaccess.local

# Backup files (contain sensitive data)
backups/*.backup
backups/*.json

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Environment-specific configuration
config.local.php
config.local.js

# Log files
*.log
logs/

# MCP configuration (contains sensitive API keys)
.mcp.json
