# Goldeneye Backup Directory Protection Example
# Copy to backups/.htaccess and customize for your deployment

# Deny all direct access to backup files
Order Deny,Allow
Deny from all

# Prevent directory listing
Options -Indexes

# Additional security headers
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "DENY"
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# Disable server signature
ServerSignature Off

# Log access attempts for security monitoring
<IfModule mod_log_config.c>
    LogFormat "%h %l %u %t \"%r\" %>s %O \"%{Referer}i\" \"%{User-Agent}i\"" backup_access
    # CUSTOMIZE: Adjust log path for your server setup
    CustomLog logs/goldeneye_backup_access.log backup_access env=!backup_api_access
</IfModule>